================================================================================
❌ ПРОБЛЕМА: Chrome не поддерживает HTTP прокси с auth через --proxy-server
================================================================================

🔍 ЧТО ОБНАРУЖЕНО:

1. В Profile-1 сохранён HTTP прокси:
   - host: mproxy.site
   - port: 12138
   - type: http
   - username: Hes9yF
   - password: zAU2vaEUf4TU

2. Код правильно передаёт прокси в browser_manager:
   proxy_data = {
       'host': 'mproxy.site',
       'port': 12138,
       'username': 'Hes9yF',
       'password': 'zAU2vaEUf4TU',
       'proxy_type': 'http'
   }

3. Browser_manager пытается настроить прокси двумя способами:
   
   СПОСОБ 1 (не работает):
   --proxy-server=http://username:password@host:port
   Результат: Chrome игнорирует credentials в URL
   
   СПОСОБ 2 (пробуем сейчас):
   --proxy-server=http://host:port + расширение для auth
   Проблема: undetected_chromedriver может не поддерживать --load-extension

4. Тест показывает:
   ✅ Браузер запускается
   ✅ Прокси конфигурируется
   ✅ Расширение создаётся
   ❌ "This site can't be reached" или пустая страница
   ❌ IP не определяется

================================================================================
💡 РЕШЕНИЯ:
================================================================================

ВАРИАНТ 1: Использовать selenium-wire (рекомендуется)
   - Библиотека перехватывает HTTP трафик
   - Встраивает proxy auth на уровне requests
   - Работает с любыми прокси
   
   pip install selenium-wire
   
   Код:
   from seleniumwire import webdriver
   options = {
       'proxy': {
           'http': 'http://username:password@host:port',
           'https': 'http://username:password@host:port'
       }
   }
   driver = webdriver.Chrome(seleniumwire_options=options)

ВАРИАНТ 2: Chrome extension через chrome_options.add_extension()
   - Создать .crx файл с расширением
   - Добавить через options.add_extension('/path/to/extension.crx')
   - Требует правильной упаковки расширения

ВАРИАНТ 3: Использовать SOCKS5 прокси БЕЗ авторизации
   - Попросить прокси без авторизации
   - Или использовать IP whitelist

ВАРИАНТ 4: Прокси через системные настройки
   - Настроить прокси на уровне ОС
   - Chrome будет использовать системные настройки

ВАРИАНТ 5: Использовать tunneling (SSH/SSHTunnel)
   - Создать локальный SOCKS тоннель без auth
   - Chrome подключается к localhost:local_port
   - Туннель пробрасывает на реальный прокси

================================================================================
🎯 РЕКОМЕНДУЕМОЕ РЕШЕНИЕ:
================================================================================

Использовать selenium-wire + undetected_chromedriver:

1. Установить: pip install selenium-wire

2. Изменить browser_manager.py:
   from seleniumwire import webdriver as wire_webdriver
   
   # Настройка прокси для selenium-wire
   if proxy_data and proxy_data.get('username'):
       seleniumwire_options = {
           'proxy': {
               'http': f"http://{proxy_data['username']}:{proxy_data['password']}@{proxy_data['host']}:{proxy_data['port']}",
               'https': f"http://{proxy_data['username']}:{proxy_data['password']}@{proxy_data['host']}:{proxy_data['port']}"
           }
       }
   
   driver = wire_webdriver.Chrome(
       options=chrome_options,
       seleniumwire_options=seleniumwire_options
   )

3. Это добавит прослойку которая автоматически добавит Proxy-Authorization заголовок

================================================================================
📝 ТЕКУЩИЙ КОД:
================================================================================

Логи показывают:
[INFO] 🔧 Configuring proxy: type=http, host=mproxy.site, port=12138
[INFO] ✅ Set proxy server: http://mproxy.site:12138
[INFO] ✅ Added proxy auth extension with user: Hes9yF

Но сайты не открываются через прокси.

Причина: Chrome не передаёт credentials автоматически для HTTP прокси.
Решение: Нужна прослойка (selenium-wire) или правильное расширение.
